#pragma once
#include <string>
#include "history.h"

//History TranslationTask(const Sentence *sentence, size_t taskCounter);
History TranslationTask(const Sentences *sentences, size_t taskCounter);

